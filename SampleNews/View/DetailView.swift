

import SwiftUI

struct DetailView: View {
    let url : String?
    
    var body: some View {
        Text("working on it!")
    }
}

struct DetailView_Previews: PreviewProvider {
    static var previews: some View {
        DetailView(url: "https://www.housing.com")
    }
}
